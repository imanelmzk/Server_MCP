model User{
  id Int @id @default(autoincrement())
  name String 
  lastName String
}
generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
